# 手机访问测试指南

本文档说明如何用手机浏览器访问运行在电脑上的本项目（深职大香蜜校区｜创新创意设计艺术学院校园指南 H5），完成移动端真机测试。

---

## 完整测试步骤

### 1. 电脑启动项目后端服务

在项目根目录下打开终端，启动服务：

```bash
# 默认端口 3000
npm start

# 若 3000 端口被占用，可指定其他端口（如 3100）
PORT=3100 npm start
```

启动后终端会提示监听地址（例如 `http://0.0.0.0:3000`）。
**注意：** 服务必须监听 `0.0.0.0`（已内置），手机才能通过局域网 IP 访问。

### 2. 电脑和手机连接【同一个 Wi-Fi】（同一局域网）

- 电脑连接到某个 Wi-Fi（例如家里的无线路由器）。
- 手机也连接到**同一个** Wi-Fi。
- 两者必须在同一个局域网内，否则无法互相访问。

### 3. 电脑查看本机局域网 IP 地址

**Windows：**
按 `Win + R`，输入 `cmd` 回车打开命令提示符，执行：

```cmd
ipconfig
```

在输出中找到「无线局域网适配器 WLAN」下的 **IPv4 地址**，例如：

```
IPv4 地址 . . . . . . . . . . . . : 192.168.3.45
```

**Mac：**
打开「终端」，执行：

```bash
ifconfig
```

在输出中找到 `en0`（或正在使用的网卡）下的 `inet` 字段，即为内网 IP，例如：

```
inet 192.168.3.45 netmask 0xffffff00 ...
```

### 4. 手机打开浏览器，输入访问地址

手机浏览器（Safari / Chrome 等）地址栏直接输入：

```
http://你的电脑IP:端口号
```

示例（假设电脑 IP 为 `192.168.3.45`，端口 `3000`）：

```
http://192.168.3.45:3000
```

> 端口号与第 1 步启动时一致（默认 `3000`，若用 `PORT=3100` 启动则为 `3100`）。

### 5. 验证访问成功

- 页面正常加载、显示首页轮播与「吃 / 学 / 玩」等模块 = **手机端访问成功 ✅**
- 可点击板块、可登录后台（右上角「管理入口」按钮）进行投稿/审核测试。

### 6. 重要提示

- **只连同一个 Wi-Fi = 仅局域网访问。** 手机和电脑必须在同一局域网才能打开。
- **手机用流量（不同网络）是打不开的**，这属于跨局域网访问，不在同一内网。
- 若需要让外网 / 异地手机也能访问，需要以下任一方案：
  - **内网穿透**（如 ngrok、frp、花生壳等），把本地端口映射为公网地址；
  - **云服务器部署**，把项目部署到具备公网 IP 的服务器（如腾讯云、阿里云等）。

---

## 常见问题排查

| 现象 | 可能原因 | 解决办法 |
| --- | --- | --- |
| 手机打不开页面 | 手机与电脑不在同一 Wi-Fi | 确认两者连接同一个无线网 |
| 手机打不开页面 | 地址/端口输错 | 核对电脑 IPv4 与启动端口 |
| 手机打不开页面 | 电脑防火墙拦截 | 放行该端口的入站规则 |
| 电脑本地能开、手机不能开 | 服务未监听 0.0.0.0 | 本项目已内置，重新 `npm start` |
| 端口被占用启动失败 | 3000 已被其他进程占用 | 用 `PORT=3100 npm start` 换端口 |

---

## 跨网络访问（内网穿透 / 云服务器）

若要让**异地手机、或用流量的手机**也能访问，需把项目暴露到公网，本项目已内置 **CORS 跨域支持**，可直接使用：

1. **内网穿透**：在电脑上运行 ngrok / frp / 花生壳，把本地端口（如 `3000`）映射为公网 https 地址，手机用该地址打开即可。
2. **云服务器部署**：把项目放到具备公网 IP 的服务器（如腾讯云、阿里云），用 Nginx 等反代并配置 HTTPS。

> 跨域登录态兼容：项目会根据外层协议自动切换 Cookie 策略——**HTTPS 环境**下登录 Cookie 设为 `SameSite=None; Secure`，可跨域携带；**HTTP 环境**下保持 `SameSite=Lax`，确保局域网/本机登录正常。前端接口全部使用相对路径、且 `fetch` 已设 `credentials: 'include'`，**换域名 / 走隧道均无需改前端代码**。
>
> 注意：若前端与接口分属不同域名，浏览器仅在 **https** 下才会写入跨域登录 Cookie，请用 https 访问前端。

